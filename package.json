{
    "name": "dev-memory-ai-code-recall",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "turbo build",
        "dev": "turbo dev",
        "lint": "turbo lint",
        "package:ext": "npm run package --workspace=devmemory-ai-code-recall"
    },
    "devDependencies": {
        "turbo": "^2.0.0",
        "typescript": "^5.0.0"
    }
}